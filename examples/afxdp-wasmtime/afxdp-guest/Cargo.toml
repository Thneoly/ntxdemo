[package]
name = "afxdp-guest"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.49.0"

# Parsing helpers (keep no_std out for now; wasip2 supports std)
bytes = "1.10.1"
