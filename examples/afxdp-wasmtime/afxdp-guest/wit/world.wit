package afxdp:demo;

interface packet {
    record packet-meta {
        frame-len: u32,
        rx-queue: u32,
    }

    on-packet: func(meta: packet-meta, data: list<u8>);
}

world guest {
    export packet;
    export run: func();
}
