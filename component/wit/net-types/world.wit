package ntx:net-types@0.1.0;

interface types {
    record ipv4-addr {
        a: u8,
        b: u8,
        c: u8,
        d: u8,
    }

    record mac-addr {
        a: u8,
        b: u8,
        c: u8,
        d: u8,
        e: u8,
        f: u8,
    }
}

world net-types {
    export types;
}