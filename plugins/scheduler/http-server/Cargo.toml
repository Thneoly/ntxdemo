[package]
name = "http-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.7", features = ["json"], optional = true }
tokio = { version = "1.39", features = ["full"], optional = true }
