[package]
name = "scheduler-actions-http"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
scheduler-core = { path = "../core-libs" }
scheduler-executor = { path = "../executor" }
serde_json = "1.0"
serde_yaml = "0.9"
ureq = { version = "2.9", features = ["json"] }
