[package]
name = "scheduler-actions-executor-client"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.49.0", features = ["realloc"] }

[package.metadata.component]
package = "scheduler:actions-executor"

[profile.release]
opt-level = "z"
lto = true
strip = true
